define(["cs!seq", "input/main", "msa/main", "utils/main"], function (seq, input, msa, utils) {
    return {
        version: "0.1",
        seq: seq,    
        input: input,
        msa: msa,
        utils: utils,
    }
});

