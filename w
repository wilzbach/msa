#!/bin/sh
# haha I am super lazy - just type ./w to start developing
# this will start
# (1) a local server (npm run sniper)
# (2) watch for file changes and recompile (npm run watch == gulp watch)

npm run w
